<%= form_for @type do |f| %>
 
  <% if @type.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@beer.errors.count, "error") %> prohibited
        this type from being saved:
      </h2>
      <ul>
        <% @beer.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
<p>
    <b><%= f.label :title, "Nazwa typu:" %></b><br>
    <%= f.text_field :title %>
  </p>
 
  <p>
    <%= f.submit "Dodaj typ"%>
  </p>
<% end %>